import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const espaceArtistiqueImporterSelectors = importerSelectors(
  'espaceArtistique.importer',
);

export default espaceArtistiqueImporterSelectors;
