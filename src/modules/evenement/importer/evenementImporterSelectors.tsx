import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const evenementImporterSelectors = importerSelectors(
  'evenement.importer',
);

export default evenementImporterSelectors;
