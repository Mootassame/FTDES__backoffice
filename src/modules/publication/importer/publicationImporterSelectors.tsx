import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const publicationImporterSelectors = importerSelectors(
  'publication.importer',
);

export default publicationImporterSelectors;
